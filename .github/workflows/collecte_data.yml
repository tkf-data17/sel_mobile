name: Weekly Data Scraper

on:
  schedule:
    - cron: "0 8 * * 1"  # Lundi à 08:00 UTC
  workflow_dispatch:      # Lancer manuellement depuis GitHub

jobs:
  scrape:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run scraper
        run: python collecte_data.py

      - name: Commit and push changes
        run: |
          git config --global user.name "tkf-data17"
          git config --global user.email "tayif01@gmail.com"
          git add .
          git commit -m "Mise à jour automatique des données : $(date '+%Y-%m-%d %H:%M:%S')" || echo "Aucun changement"
          git push
